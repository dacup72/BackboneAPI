<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h3>Products</h3>
      <ul>
        {{#each products}} 
        <li>{{this.name}}</li>
        {{/each}}
      </ul>
    </div>
  </div>
</div>